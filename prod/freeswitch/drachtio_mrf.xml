<profile name="drachtio_mrf">
  <!--
    PRODUCTION MRF profile for VPS deployment.
    Identical to docker/freeswitch_configs/sip_profiles/drachtio_mrf.xml EXCEPT
    ext-rtp-ip / ext-sip-ip are set to the VPS public IP so RTP/media reaches
    browsers over the internet. Mounted over the shared file only in prod.

    >>> REPLACE 77.237.241.24 below if your public IP changes. <<<
  -->
  <domains>
    <domain name="all" alias="true" parse="false"/>
  </domains>

  <settings>
    <param name="sip-trace" value="no"/>
    <param name="rfc2833-pt" value="101"/>
    <param name="dtmf-type" value="rfc2833"/>
    <param name="inbound-late-negotiation" value="true"/>
    <param name="rtp-ip" value="$${local_ip_v4}"/>
    <param name="sip-ip" value="$${local_ip_v4}"/>
    <param name="sip-port" value="5090"/>
    <param name="ext-rtp-ip" value="77.237.241.24"/>
    <param name="ext-sip-ip" value="77.237.241.24"/>
    <param name="auth-calls" value="false"/>
    <param name="enable-timer" value="false"/>
    <param name="user-agent-string" value="Enjoys media gateway"/>
    <param name="username" value="Enjoys-Mediaserver"/>
    <param name="apply-nat-acl" value="nat.auto"/>
    <param name="context" value="mrf"/>
    <param name="rtp-timer-name" value="soft"/>
    <param name="codec-prefs" value="$${global_codec_prefs}"/>
    <param name="inbound-codec-negotiation" value="greedy"/>
    <param name="inbound-reg-force-matching-username" value="false"/>
    <param name="nonce-ttl" value="86400"/>
    <param name="manage-presence" value="false"/>

    <!-- Accept DTLS-SRTP (WebRTC) offers relayed by drachtio-fsmrf so browser
         clients can reach the IVR; fall back to plain RTP for softphones.
         Without this, FreeSWITCH fails WebRTC INVITEs with
         "Crypto not negotiated but required" / 488 => no audio on the VPS. -->
    <param name="rtp-secure-media" value="optional"/>

    <!-- Accept the browser's ICE candidates. WebRTC clients send RFC1918
         (192.168.x / 172.x / 10.x) and host candidates. The default ACL is
         wan.auto (public IPs only), so all candidates get filtered out
         => "no suitable candidates found" => 488. These ACLs (incl. the
         any_v4.auto catch-all) let FreeSWITCH use the offered candidates. -->
    <param name="apply-candidate-acl" value="localnet.auto"/>
    <param name="apply-candidate-acl" value="wan_v4.auto"/>
    <param name="apply-candidate-acl" value="rfc1918.auto"/>
    <param name="apply-candidate-acl" value="any_v4.auto"/>

    <!-- so we can send INVITEs with no SDP and get an offer back with all available codecs -->
    <param name="enable-3pcc" value="true"/>

    <param name="tls" value="false"/>
  </settings>
</profile>
